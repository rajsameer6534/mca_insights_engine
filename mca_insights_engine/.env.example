# Copy this to .env and fill in if you want to enable optional LLM or proxies.
OPENAI_API_KEY=
HTTP_PROXY=
HTTPS_PROXY=
# Set to 'true' to allow live web enrichment (scraping/APIs). Default is false (uses seeded enrichment).
ENABLE_WEB_ENRICHMENT=false
